default:
  context:
    parameters:
        base_url: http://api.ontheroad.dev
        root_dir: /var/www/api
        cli_env: local
        users: {c.desneuf@gmail.com: password, paul@test.fr: paulPassword}